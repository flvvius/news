// Strings moved to the shared @news-app/i18n package (also consumed by
// apps/native). This module stays as a re-export so existing imports keep
// working.
export {
  getString,
  STRINGS,
  type Locale,
  type StringKey,
} from "@news-app/i18n";
